// problem (a)
#include <stdio.h>
int main(){
int x,y;
scanf("%d",&x);
    if(x>0)
    {
    y=1;
        if(x=0)
        {
        y=0;
            if(x<0)
            {
            y=-1;
            }
        }
    }
    else
    {
    printf("Enter an integer number");
    }    
    printf("y= %d",y);    
}    
