// Using Condition and Loop

#include <stdio.h>
int main()
{
    int num=101,sum = 0;
    while(num<200)
    {
        if(num%7==0)
        {
          sum+=num;
    }
        num++;
    }
    printf("The number is %d\n",sum);
}
