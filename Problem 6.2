// Using Condition and Loop
#include <stdio.h>
int main()
{
    int num=101,sum;
    while(num<200)
    {
        sum+=num;
        num++;
        if(sum%7==0)
            {
        printf("The number is %d\n",sum);
        }
    }
}
