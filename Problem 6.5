#include <stdio.h>
int main()
{
    int math,phy,che,all_sub,mat_and_phy;
    scanf("%d %d %d",&math,&phy,&che);
    if(math>=60 && phy>=50 && che>=40)
    {
        all_sub=math+phy+che;
        mat_and_phy=math+phy;
        if(all_sub>=200 || mat_and_phy>=150)
        {
            printf("The person is Eligible Candidate");
        }
    }
    else
    {
       printf("The person is not Eligible Candidate");
    }
}
